# Session Secret (required) - Generate a strong random string
# Use: openssl rand -base64 32
SESSION_SECRET=your-session-secret-here-change-me

# Firebase/GCP Configuration

# Option 1: Service Account Key File Path (local development)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Option 2: Inline Credentials (useful for some deployment scenarios)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Option 3: Firestore Emulator (local development without GCP)
# FIRESTORE_EMULATOR_HOST=localhost:8080

# For Cloud Run deployment (automatically set)
# GOOGLE_CLOUD_PROJECT=your-project-id
# K_SERVICE=your-service-name (automatically set by Cloud Run)

# Resend (email provider)
# RESEND_API_KEY=re_1234567890abcdef
# RESEND_FROM_EMAIL="GCSE Quiz <no-reply@your-domain.com>"

# Apple App Store Server API (subscriptions)
# APPLE_IAP_KEY_ID=your-key-id
# APPLE_IAP_ISSUER_ID=your-issuer-id
# APPLE_IAP_BUNDLE_ID=com.your.app
# APPLE_IAP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# APPLE_IAP_ENV=sandbox

# Google Play Developer API (subscriptions)
# GOOGLE_PLAY_PACKAGE_NAME=com.your.app
