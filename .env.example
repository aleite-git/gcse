# =============================================================================
# GCSE Quiz - Environment Variables
# =============================================================================

# -----------------------------------------------------------------------------
# Session
# -----------------------------------------------------------------------------

# Session secret for signing cookies (required)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-here-change-me

# -----------------------------------------------------------------------------
# Firebase / GCP Configuration
# -----------------------------------------------------------------------------

# Option 1: Service Account Key File Path (local development)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Option 2: Inline Credentials (useful for some deployment scenarios)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Option 3: Firestore Emulator (local development without GCP)
# FIRESTORE_EMULATOR_HOST=localhost:8080

# Automatically set by Cloud Run (do not set manually in production)
# GOOGLE_CLOUD_PROJECT=your-project-id
# K_SERVICE=your-service-name

# -----------------------------------------------------------------------------
# Email (Resend)
# -----------------------------------------------------------------------------

# Resend API key for sending transactional emails
# RESEND_API_KEY=re_1234567890abcdef

# Sender address shown in outgoing emails
# RESEND_FROM_EMAIL="GCSE Quiz <no-reply@your-domain.com>"

# -----------------------------------------------------------------------------
# Auth - Mobile OAuth
# -----------------------------------------------------------------------------

# Google OAuth client ID for mobile sign-in
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Apple OAuth client ID (Services ID) for mobile sign-in
# APPLE_CLIENT_ID=com.your.app.service

# -----------------------------------------------------------------------------
# Subscription - RevenueCat
# -----------------------------------------------------------------------------

# RevenueCat API key for server-side subscription lookups
# REVENUECAT_API_KEY=your-revenuecat-api-key

# Shared secret to authenticate incoming RevenueCat webhooks
# REVENUECAT_WEBHOOK_AUTH=your-webhook-secret

# Custom header name RevenueCat uses for auth (default: authorization)
# REVENUECAT_WEBHOOK_AUTH_HEADER=authorization

# RevenueCat entitlement identifier (default: premium)
# REVENUECAT_ENTITLEMENT=premium

# -----------------------------------------------------------------------------
# Subscription - Apple App Store Server API
# -----------------------------------------------------------------------------

# APPLE_IAP_KEY_ID=your-key-id
# APPLE_IAP_ISSUER_ID=your-issuer-id
# APPLE_IAP_BUNDLE_ID=com.your.app
# APPLE_IAP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# APPLE_IAP_ENV=sandbox

# -----------------------------------------------------------------------------
# Subscription - Google Play Developer API
# -----------------------------------------------------------------------------

# GOOGLE_PLAY_PACKAGE_NAME=com.your.app

# -----------------------------------------------------------------------------
# Admin
# -----------------------------------------------------------------------------

# Comma-separated list of admin email addresses
# ADMIN_EMAILS=admin@example.com

# JWT audience claim for admin override tokens
# ADMIN_OVERRIDE_AUDIENCE=https://your-app-url.run.app

# Shared secret to authenticate scheduled cron requests
# CRON_SECRET=your-cron-secret

# -----------------------------------------------------------------------------
# Deployment / Build
# -----------------------------------------------------------------------------

# Git commit SHA injected at build time (set automatically by Cloud Build)
# COMMIT_SHA=abc1234
